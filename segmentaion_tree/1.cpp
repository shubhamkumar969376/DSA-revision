// range update and range query using segment tree
#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<int> arr(n);
    for(int i=0;i<n;i++) cin>>arr[i];
    int q;
    cin>>q;

    while(q--){
        int ch;
        cin>>ch;
        if(ch==1){

        }

        else {
        

        }

    }
}